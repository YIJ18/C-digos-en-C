#include<stdio.h>

int main(void){

  char arreglo[20][20];
  int y;

  for(y=0;y<20;y++){
  scanf("%[^\n]", arreglo[y]);
  getchar();
  }
  for(y=0;y<20;y++){
  printf("%s\n", arreglo[y]);
  }
  return 0;
}
