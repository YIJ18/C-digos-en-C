/*
Escriba un programa que permita calcular la cantidad de días transcurridos entre dos fechas.
El programa solicita dos fechas, las cuales debe validar. También, la primera fecha debe ser
“menor” que la segunda. Use funciones. Tip: Para la validación de las dos fechas, basta con
una sola función (con argumentos) que sea llamada en dos ocasiones, pero con diferente
fecha cada vez.
*/

#include <stdio.h>
#include <stdio_ext.h>
#include <string.h>

int main (void)
{
    return 0;
}